# The Campaign/CampaignSMTPRoute models that used to live here have been
# merged into apps.sequences (renamed Sequence -> Campaign, see that app's
# models.py). This app now only houses shared send infrastructure
# (scheduling.py, services.py) used across the codebase.
